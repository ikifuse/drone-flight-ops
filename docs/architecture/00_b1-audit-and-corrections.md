# Phase B1.1 再監査・技術的訂正報告書（00_b1-audit-and-corrections.md）

最終更新: 2026-09-14
プロジェクト: `drone-flight-ops`
フェーズ: Phase B1.1（アーキテクチャ比較・検討の再監査および記述訂正）

---

## 1. 再監査の目的と位置づけ

Phase B1で提示した「PWA ＋ バックエンド境界 ＋ ローカル優先保存 ＋ Googleスプレッドシート連携」という方向性は有力な第一候補ですが、B1ドキュメント内に**「断定が強すぎる記述」「未確認事項を確定扱いした記述」「内部矛盾」**が存在していました。

本フェーズ（B1.1）は、B2（詳細設計）へ進む前にこれらを徹底的に再監査し、
1. **【確認済み事実】**: 公式資料等で裏付けが取れている事項
2. **【技術上の推奨・設計目標】**: 事実を踏まえ、本プロジェクトにおいて目指すべき方針
3. **【未確認事項】**: 国交省DIPS仕様等、現時点で推測で断定してはならない事項
4. **【将来変更可能性】**: 料金体系、無料枠、OSやブラウザの仕様変更リスク
を厳格に分離・訂正することを目的とします。

---

## 2. 主要な再検証・訂正項目一覧

### 2.1 「永久無料」「完全0円」表現の撤回と正確な料金評価
- **訂正前**: 「Cloudflare Workersは永久無料」「完全0円で永久運用可能」「年間費用完全0円」など、将来にわたる固定費用ゼロを断定。
- **確認された事実（2026-09-14時点）**:
  - **Cloudflare Workers**: 無料プラン（Free Plan）は現在 **「1日100,000リクエスト」**、CPU時間「10ms/リクエスト」が無償提供されている。個人1名による年間数百フライト・1日数回のアクセスであれば、現在の無料枠内に余裕で収まる可能性が高い。
  - **将来リスク**: クラウドベンダーの無料枠規約や料金体系は将来改定される可能性があるため、「永久に0円」と保証することは技術的・経営的に誤りである。
- **訂正後の方針**: 「現時点の公式無料枠および想定利用量に照らすと、月額費用0円で運用できる可能性が極めて高い」という表現に改める。また、無料枠を超過した場合の従量課金やプラン変更の可能性を明記する。

### 2.2 Cloudflare Workersの性能表現の適正化
- **訂正前**: 「コールドスタート0ms」「ミリ秒起動」「必ず安全」などの誇大・固定値表現。
- **確認された事実**:
  - Cloudflare WorkersはV8 Isolate技術を採用しており、一般的なコンテナ型サーバーレス（数秒〜数百ms）と比較して起動オーバーヘッドが極めて小さい（数ミリ秒以下）。しかし、ネットワークレイテンシやハンドシェイクを含めた「0ms保証」は存在しない。
- **訂正後の方針**: 「0ms保証」という表現を撤回し、「V8 Isolateによる極めて低遅延な起動特性」として設計上のメリットを客観的に記述する。

### 2.3 PWAおよびIndexedDBの耐久性・データ消失リスクの正確な位置づけ
- **訂正前**: 「iOS Safariのキャッシュ削除リスクはIndexedDBへの二重保存で回避可能」「IndexedDBなら安心」という単純化。
- **確認された事実**:
  - **WebKit（iOS）のストレージ管理**: SafariおよびWebKitでは、端末の空きストレージ容量が逼迫した場合、OS側でストレージ領域（Cache Storage、IndexedDB等を含む）が自動削除（Eviction）される対象となり得る。
  - **ホーム画面追加PWAの扱い**: ホーム画面に追加されたWebアプリはブラウザ通常タブより永続性が優遇される傾向にあるが、ユーザーによるSafari閲覧履歴の消去操作や端末故障・OSの不具合によって消失するリスクはゼロではない。
- **訂正後の方針**: 「IndexedDBに保存すれば絶対に消えない」という断定を撤回。端末内IndexedDBは**「現場での一時作業領域・ローカルキャッシュ」**として位置づけ、端末故障やEvictionに備えて外部（スプレッドシートやバックアップファイル）への退避・同期を必須とする設計とする。

### 2.4 「オフライン100%」「絶対に止まらない」の前提条件の明確化
- **訂正前**: 「完全圏外でも100%動作」「絶対に止まらない」「オフライン完全保証」。
- **確認された事実**:
  - 初回アクセス前（Service Workerがインストールされていない状態）、キャッシュ更新の途中、端末ストレージ枯渇時、OSによる強制終了時などはオフライン起動できない場合がある。
- **訂正後の方針**: 「絶対」「100%」を撤回し、**「一度オンラインで正常に読み込み・必要データを取得・キャッシュ完了した状態を前提として、通信不要な現場業務（点検、離着陸、バッテリー交換、日誌入力）をオフラインで継続できるよう設計する」**という実現可能な保証範囲と前提条件を明記する。

### 2.5 「1ms保存」等の固定性能値の撤回
- **訂正前**: 「1ミリ秒で打刻・保存」「ミリ秒永続化」。
- **訂正後の方針**: 実装前の段階で固定数値を保証することをやめ、「ネットワーク通信の往復待ち（数秒）を発生させず、端末内ローカルストレージへ即時に近い形で永続化する設計目標」と表現を改める。

### 2.6 Capacitorへの移行表現の適正化
- **訂正前**: 「Webコードを100%そのまま流用可能」「1行も書き直すことなくネイティブアプリ化」。
- **確認された事実**:
  - CapacitorによってWeb資産のUIや主要ロジックを大幅に再利用できることは事実だが、ネイティブプラグインの初期化、iOS/Androidの権限要求（位置情報等）、Safe Area（ノッチ・ホームバー）対応、ファイル保存先パスの差異など、ネイティブシェル化に伴う追加実装・調整は必ず発生する。
- **訂正後の方針**: 「100%無修正」を撤回し、「WebフロントエンドのUI・業務ロジックの大部分を再利用でき、将来ネイティブアプリへ拡張する際の移行コストを低く抑えられる」という現実的な表現に改める。

### 2.7 Apple Developer費用・配布方式の整理
- **訂正前**: 「年間12,980円が毎年必須」「Flutter/Expoは必ず有料契約が必要」。
- **確認された事実**:
  - **無料Apple Account**: MacとUSB接続すれば無料のApple IDで個人開発用としてiPhone実機にアプリをインストール可能。ただし有効期限が「7日間」で切れ、最大3アプリまでという厳しい制約があるため、ドローンの実現場運用としては実用的ではない。
  - **有料Apple Developer Program**: 年額99米ドル（為替変動により日本円価格は改定される。2026年時点では約13,000円前後）。TestFlight（90日間配布）やApp Store公開に必須。
- **訂正後の方針**: 固定金額の断定を避け、為替や規約変更があり得ることを明示。PWAであればこれらの審査・署名更新・費用を回避してiPhoneでフルスクリーン起動できる点を公平に評価する。

### 2.8 クロスプラットフォーム（Flutter / React Native / Expo）評価の客観化
- **訂正前**: PWAを有利に見せるため、Flutter/Expoのデメリットを強調しすぎた。
- **訂正後の方針**:
  - **Expo / React Native**: TypeScript資産を共有でき、Web・iOS・Androidを単一コードで扱える強力な選択肢。
  - **Flutter**: 描画性能とUIの一貫性に優れるが、Dart言語への依存とWeb地図エコシステム（MapLibre JS等）の流用性に課題がある。
  - 個人運用の観点から「ストア公開・ビルド環境保守の手間」を理由にPWAを第1候補とする客観的なロジックに修正する。

### 2.9 Supabase無料枠休止仕様の正確化
- **訂正前**: 「1週間無アクセスで必ず停止する」。
- **確認された事実**:
  - Supabase Free Planでは、7日間アクティブなリクエスト（API呼び出しやDBアクセス）がないプロジェクトは自動的に「一時停止（Paused）」状態となる。ダッシュボードから手動で再開（Restore）できるが、復帰に数分を要する。
- **訂正後の方針**: 単に「使えない」と断じるのではなく、「数週間ぶりに現場でドローンを飛ばすような利用パターンの場合、現場で即座に通報・同期できない運用リスクがある」と正確に評価する。

### 2.10 GASのオフライン評価の精緻化
- **訂正前**: 「現行GASではオフライン完全動作が構造上不可能」。
- **訂正後の方針**: GASの「HTML Service」自体がサーバー通信を前提とする点と、フロントをPWAにしてバックエンドAPIとしてGASを呼び出す別構成を区別し、現場オフラインでの起動・保存を端末内で担保する必要性を正確に述べる。

### 2.11 DIPS2.0認証・連携仕様の未確認事項への完全回帰
- **訂正前**: client_secret、refresh token、OIDC認可コードフローを確定仕様のように記述し、Cloudflare Workersでの秘匿を前提化。
- **確認された事実**:
  - 国交省の公開資料にOIDCの記述はあるものの、API利用申請主体（個人可否）、client_id/secretの発行単位、トークン有効期限などは未確定である。
  - ブラウザからの直接通信（CORS対応やPKCEサポートの有無）も未確定である。
- **訂正後の方針**:
  - DIPS認証仕様を推測で確定しない。
  - 「将来DIPS APIの秘密情報や資格情報を安全に扱う必要がある場合に備え、クライアントとDIPSの間にバックエンド境界（プロキシ）を設ける設計が安全側であり有力である」という技術方針として位置づける。

### 2.12 【最重要】ローカルDBとGoogleスプレッドシートの「正本」矛盾の解消
- **訂正前の矛盾**:
  - ある箇所では「ローカルDB（IndexedDB）を正本（SSoT）とする」と書き、別の箇所では「データ最終原本・監査台帳 ＝ Googleスプレッドシート」と記載しており、どちらが権威ある正本なのか矛盾していた。
- **訂正後の整理（4つの選択肢とB2での設計方針）**:
  - B1段階で無理に「スプレッドシートが最終原本」と固定しない。
  - 以下の選択肢を提示し、B2でデータのライフサイクル（作成中・未同期・確定・手修正・監査履歴）を含めて詳細設計する。
    - **選択肢A: ローカルDBが正本**（スプレッドシートは閲覧・出力ビュー）
    - **選択肢B: スプレッドシートが正式原本**（ローカルDBは現場一時記録・同期待ちキュー）
    - **選択肢C: クラウドDBが正本**（端末およびスプレッドシートはクライアント）
    - **選択肢D: 複合的な権威管理**（現場の離着陸実績は端末で確定、法定台帳・機体原本累計はスプレッドシート側で確定させ、相互の状態を追跡する）

### 2.13 地図・空域データの利用規約とオフライン条件の分離
- **訂正前**: 国土地理院タイルやDIDデータにより「地図API費用が完全0円で揃う」と一体化して断定。
- **確認された事実**:
  - 国土地理院タイル（標準地図・写真等）は規約に基づき無償利用可能だが、大量キャッシュのオフライン配布には利用規約の遵守が必要。
  - ドローン規制空域（空港周辺空域、緊急用務空域、進入表面等）の最新データは、地理院タイル単独では完結せず、国交省航空局の告示や外部データソースの統合が必要。
- **訂正後の方針**: 地図タイル表示と空域規制データソースを分離し、空域データの取得方式・キャッシュ可否はB2以降の技術課題として正確に整理する。

### 2.14 PWAのiPhone実用性制約の客観的整理
- **訂正前**: 「ホーム画面追加でネイティブと全く同様に動作する」という誇大表現。
- **訂正後の方針**:
  - iOSにおけるPWAの制約（Web Push通知のiOS要件、バックグラウンドでの長時間処理不可、画面ロック復帰時の再描画、カメラ・ファイルアクセス時のUIダイアログ挙動など）を明記し、本アプリの要件（現場手動打刻、地図操作、PDF出力）の範囲内であれば十分に実用的であるという条件付きの評価とする。

---

## 3. 再監査後の第一候補（推奨アーキテクチャ）の位置づけ

再監査の結果、**「PWA ＋ バックエンド境界（Cloudflare Workers等） ＋ ローカルファースト保存 ＋ Googleスプレッドシート等への外部連携」** という方向性は、依然として本プロジェクトに最も適した**「第一候補」**として維持します。

ただし、**「絶対的な解」ではなく、以下の前提条件を満たす場合に最も優位性を持つ「条件付き第一候補」**として再定義します。

### 第一候補を維持する前提条件
1. **個人利用・1人運用**であり、ストア公開やApple Developer年会費の支払いを避けたいこと。
2. **Web地図エコシステム（MapLibre GL JS、GeoJSON等）**を活用して円・ポリゴン描画を迅速に構築したいこと。
3. **現場でのオフライン作業**（通信圏外での点検、離着陸記録、バッテリー交換）が必須であること。
4. 単一のTypeScriptコードベースにより、**AIアシスタントを活用した迅速・低コストな保守**を行いたいこと。

### 将来、第2候補（Capacitor等）へ見直すトリガー（条件崩れ）
- バックグラウンドでのGPS連続軌跡トラッキングが必須要件となった場合。
- 機体（ドローン本体）との直接的なBluetooth通信やローカルハードウェア連携が必要となった場合。
- オーナーが一般公開（App Store / Google Play）を望むようになった場合。
- iOSのSafari/PWA仕様が改変され、IndexedDBの永続性やオフライン運用が著しく制限された場合。
