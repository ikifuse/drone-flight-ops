# バックエンド候補とセキュリティ比較（02_backend-and-security-comparison.md）

最終更新: 2026-09-14
プロジェクト: `drone-flight-ops`

---

## 1. バックエンドが必要となる背景と設計上の責務

本アプリにおいて、クライアント（スマートフォン）単体で完結させず、バックエンド境界を設けることを検討する主な理由は以下の3点です。

1. **将来のDIPS 2.0 API秘密情報（client_secret等）の保護**:
   - DIPS APIの正式な認証仕様は現時点で未確認ですが、もし機密性の高いAPIキーや `client_secret` が発行される場合、これらをブラウザや端末内に置くことは漏洩リスクがあります。そのため、**秘密情報をサーバー側に安全に隔離し中継できるバックエンド境界**を用意することが安全側の設計となります。
2. **CORS（Cross-Origin Resource Sharing）制限の回避**:
   - ブラウザから直接国交省DIPS APIへアクセスする場合、DIPS側がCORSヘッダーを許可していなければ通信が遮断されます。自前の中継プロキシを介することで、ブラウザのCORS制約を受けずに安全にAPI通信を中継できます。
3. **データバックアップとマルチデバイス同期**:
   - 将来的にiPhone、Pixel、iMac間で運航記録や機体台帳を共有・同期するための通信ハブとしての役割。

---

## 2. バックエンド候補マトリクス

| 評価項目 | Cloudflare Workers / Pages | Firebase (GCP) | Supabase | Vercel Serverless | GAS Backend | 自前VPS (Linux) |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| **秘密情報（secret）管理** | ◎ (暗号化Secrets機能) | ◎ (Secret Manager) | ◎ (Vault/Secrets) | ◎ (Env Secrets) | ◯ (ScriptProperties) | △ (手動設定・保守) |
| **DIPS APIとの親和性** | ◎ (高速プロキシ/Fetch) | ◯ (Node.js Functions) | ◯ (Deno Edge Functions) | ◯ (Node/Edge) | △ (UrlFetchApp制約) | ◯ (完全自由) |
| **無料枠の広さ（2026年時点）** | **◎ (1日10万req無償)** | ◯ (十分広いがカード要) | ◯ (無料枠あるが休止あり) | ◯ (非商用向け枠あり) | **◎ (無料枠内)** | ✕ (月額500〜1000円) |
| **コールドスタート遅延** | **◎ (V8 Isolate超低遅延)** | △ (数秒のコールドあり) | ◯ (Edgeなら高速) | ◯ (比較的低遅延) | ✕ (数秒〜十秒待機) | ◎ (常時稼働) |
| **個人運用の容易さ** | **◎ (メンテフリー)** | ◯ (Google管理画面) | ◯ (GUIが使いやすい) | ◎ (GitHub連携が容易) | ◎ (既存資産流用) | ✕ (OS・パッチ・保守重) |
| **障害復旧・稼働率** | ◎ (世界規模エッジ) | ◎ (Googleインフラ) | ◯ (AWS基盤) | ◎ (AWS/Cloudflare) | △ (Google障害時影響) | △ (自己責任) |
| **Vendor Lock-in度** | ◯ (標準JS/Fetch準拠) | ✕ (Firebase SDK依存) | ◯ (PostgreSQL標準) | ◯ (標準Node) | ✕ (GAS独自仕様) | ◎ (なし) |
| **GitHub連携・自動デプロイ** | ◎ (Pages/Workers CI) | ◯ (CLI/Actions) | ◯ (Actions) | ◎ (容易) | ◯ (clasp/Actions) | △ (SSH/CI設定必要) |

---

## 3. 各候補の詳細評価

### 候補1: Cloudflare Workers + Pages（最有力推奨候補）
- **特徴**: グローバルエッジ環境。Webフロントエンド（Pages）とサーバーレスAPI（Workers）を同一基盤で統合管理可能。
- **長所**:
  - **現行無料枠が個人利用に十分**（公式仕様: 1日100,000リクエスト無料。1人のドローン運航記録の利用量であれば無料枠内に十分収まる見込み）。
  - クレジットカード登録なしで開始可能。
  - V8 Isolate実行モデルにより、一般的なコンテナ型サーバーレスに見られる数秒のコールドスタートを回避し、現場で極めて低遅延に応答可能。
  - `wrangler secret` により、将来のDIPS認証情報や暗号化キーを安全に隔離・保管可能。
  - 標準のWeb `fetch` APIで書けるため、中継プロキシの実装が極めて軽量。
- **短所・注意点**:
  - 無料プランではCPU時間上限（10ms/リクエスト）がある（ただし中継プロキシ処理には十分）。
  - 将来のCloudflare側の規約・無料枠改定のリスクはゼロではない。

### 候補2: Firebase (Google Cloud)
- **長所**: Googleアカウント連携が強力。リアルタイム同期やオフラインSDKが手厚い。
- **短所**: 外部API（DIPS等）へアウトバウンド通信を行う場合、無料のSparkプランでは制約があり、従量課金プラン（Blazeプラン：カード登録必須）への移行が必要となる。

### 候補3: Supabase
- **長所**: PostgreSQLベースであり、複雑なリレーションのデータ整合性を強固に保てる。
- **短所**:
  - **無料枠の休止仕様**: Free Planでは7日間アクティブなリクエストがないプロジェクトは自動的に一時停止（Paused）される公式仕様がある。ダッシュボードから再開可能だが復帰に時間を要するため、数週間ぶりに現場でドローンを飛ばす際に即座に通報・同期できないリスクがある。

### 候補4: 現行GAS Backend（Google Apps Script）
- **長所**: 既存アプリの資産を直結でき、追加インフラ費用がかからない。
- **短所**:
  - コールドスタート（初期起動）に5〜10秒以上の待機時間が発生することが多く、現場での軽快な操作感を損ねやすい。
  - DIPSのOAuthリダイレクト処理やHTTPヘッダー制御に独自の制限がある。

### 候補5: 自前サーバー / VPS
- **短所**: 月額費用（最低500円〜1,000円程度）が恒常的に発生し、OSアップデート、セキュリティパッチ管理、SSL更新などの個人運用負荷が過大であるため非推奨。

---

## 4. バックエンド選定の評価結論

- **推奨方針**: **Cloudflare Workers + Cloudflare Pages** を第一候補とする。
- **推奨理由**:
  1. **現行仕様における費用の適合性**: 個人利用の想定トラフィックであれば、現行の無料枠内で十分に運用可能。
  2. **秘密情報の安全な隔離**: クライアント側に機密情報を露出させず、エッジ側のSecretsで隠蔽できる。
  3. **現場での応答性**: V8 Isolateによる起動遅延の極小化。
  4. **無アクセス休止リスクの回避**: Supabaseのような無アクセス停止ペナルティがない。
  5. **疎結合性**: 標準Fetch通信で構築するため、将来別のバックエンドへ移行する際の影響範囲を最小限に抑えられる。
